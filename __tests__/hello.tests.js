/**
 * A simple test before the lib implementation
 * It's purpose is to present the test passing and workflow running correctly
 */
test("adds 1 + 2 to equal 3", () => {
  expect(1 + 2).toBe(3);
});
